---
import { about } from '../data/site';
import SamsungCard from './SamsungCard';
---

<section id="origin" class="px-8 md:px-20 lg:px-32 pt-24 pb-24 md:pb-28">
  <div class="max-w-[1200px] mx-auto">

    <!-- Section entry: thin rule + label -->
    <div class="mb-16 md:mb-20">
      <hr class="border-t border-muted/20" />
      <div class="flex justify-between items-baseline mt-4">
        <p class="about-label text-muted text-xs tracking-[0.2em] uppercase">Origin</p>
        <span class="text-muted/25 text-xs tracking-[0.2em] font-body">01</span>
      </div>
    </div>

    <!-- Grid: narrative left (7 cols), Samsung credential right (5 cols) — fills the full width -->
    <div class="grid grid-cols-12 gap-x-10 lg:gap-x-16 items-start">

      <!-- LEFT — narrative, human note -->
      <div class="col-span-12 lg:col-span-7">

        <div class="about-narrative space-y-6 mb-10">
          {about.narrative.map((para) => (
            <p class="text-text text-xl md:text-2xl leading-[1.72] font-body font-light">{para}</p>
          ))}
        </div>

        <!-- Human note -->
        <p class="about-human text-muted/80 text-base leading-[1.75] font-body font-light border-t border-muted/15 pt-8">
          {about.human}
        </p>

      </div>

      <!-- RIGHT — Samsung credential card, fills its column -->
      <div class="about-samsung col-span-12 lg:col-span-5 mt-14 lg:mt-0">
        <div class="lg:sticky lg:top-24">
          <SamsungCard
            client:load
            context={about.samsung.context}
            stat={about.samsung.stat}
            subtext={about.samsung.subtext}
            courses={about.samsung.courses}
          />
        </div>
      </div>

    </div>

  </div>

</section>
